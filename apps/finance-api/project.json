{
  "name": "finance-api",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "apps/finance-api/src",
  "projectType": "application",
  "targets": {
    "build": {
      "executor": "@nx/webpack:webpack",
      "outputs": ["{options.outputPath}"],
      "defaultConfiguration": "production",
      "options": {
        "target": "node",
        "compiler": "tsc",
        "outputPath": "dist/apps/finance-api",
        "main": "apps/finance-api/src/main.ts",
        "tsConfig": "apps/finance-api/tsconfig.app.json",
        "webpackConfig": "apps/finance-api/webpack.config.js"
      },
      "configurations": {
        "development": {},
        "production": {}
      }
    },
    "serve": {
      "executor": "@nx/js:node",
      "defaultConfiguration": "development",
      "options": {
        "buildTarget": "finance-api:build"
      },
      "configurations": {
        "development": {
          "buildTarget": "finance-api:build:development"
        },
        "production": {
          "buildTarget": "finance-api:build:production"
        }
      }
    },
    "lint": {
      "executor": "@nx/eslint:lint",
      "outputs": ["{options.outputFile}"]
    },
    "test": {
      "executor": "@nx/jest:jest",
      "outputs": ["{workspaceRoot}/coverage/{projectRoot}"],
      "options": {
        "jestConfig": "apps/finance-api/jest.config.ts"
      }
    }
  },
  "tags": []
}
